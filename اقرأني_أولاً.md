# تطبيق أندرويد - ديليفري اذنا (WebView)

هذا مشروع Android Studio جاهز يفتح موقعك المرفوع مباشرة:
**https://food-delivery-jgy7.onrender.com/**

## خطوات التشغيل

1. فك ضغط الملف.
2. افتح **Android Studio** → File → Open → اختر مجلد `FoodDeliveryAdhna`.
3. انتظر حتى ينتهي "Gradle Sync" (أول مرة بياخد وقت لأنه بيحمل مكتبات).
   - إذا ظهرت رسالة عن "Gradle Wrapper" مفقود، اضغط على "OK" ليقوم Android Studio بإنشائه تلقائياً (يحتاج اتصال إنترنت لمرة واحدة).
4. وصّل جهاز أندرويد حقيقي (فعّل خيارات المطورين + USB Debugging) أو شغّل محاكي (Emulator).
5. اضغط زر ▶ Run.

## التحديث التلقائي عند تحديث Render

بما إن التطبيق بيفتح رابط موقعك مباشرة (مش نسخة محفوظة جواه)، أي تحديث تعمله على Render بينعكس تلقائياً بدون ما تحتاج تبني APK جديد، وهذا مضبوط بثلاث طبقات:

1. **بدون كاش نهائياً**: التطبيق مضبوط يجيب دايماً آخر نسخة من الخادم (`LOAD_NO_CACHE`) بدل ما يحتفظ بنسخة قديمة على الجهاز.
2. **تحديث تلقائي عند الرجوع للتطبيق**: لو المستخدم سكّر التطبيق ع الخلفية (زر الهوم) ورجع فتحه بعدين، التطبيق بيعمل reload تلقائي حتى يضمن إنه شايف آخر نسخة منشورة.
3. **سحب للتحديث يدوياً**: اسحب الشاشة للأسفل بأي وقت وهو رح يحدّث فوراً.

⚠️ ملاحظة: هذا بيحدّث **واجهة الموقع نفسها** (HTML/CSS/JS) لما تنشر تحديث على Render. أما لو حبيت تغيّر حاجات بالتطبيق نفسه غير مرتبطة بالموقع (متل الأيقونة، اسم التطبيق، الصلاحيات) فهاي لازم تبني APK جديد.

## بناء APK بدون تحميل Android Studio (عبر GitHub Actions)

فيه طريقة تبني فيها ملف APK كامل "بالسحابة" (على سيرفرات GitHub) بدون ما تحمّل أي برنامج على جهازك، بس بتحتاج حساب GitHub مجاني.

### 1) أنشئ حساب ومستودع (Repository)
- روح لـ github.com/join وأنشئ حساب مجاني (لو ما عندك).
- من الصفحة الرئيسية اضغط الزر الأخضر **New** لإنشاء مستودع جديد.
- سمّيه مثلاً `food-delivery-android`، اختر **Public** أو **Private** (الاثنين بيشتغلو مع GitHub Actions مجاناً)، **ما تحطش علامة** على "Add a README file"، واضغط **Create repository**.

### 2) ارفع ملفات المشروع
- بصفحة المستودع الفاضي، بيظهرلك رابط **"uploading an existing file"** — اضغط عليه.
- فك ضغط `FoodDeliveryAdhna-Android.zip` على جهازك، وبعدين اسحب **كل محتويات مجلد FoodDeliveryAdhna** (الملفات والمجلدات اللي جواه، مش المجلد نفسه) على منطقة الرفع بالمتصفح.
  - ⚠️ مجلد `.github` (اللي فيه إعدادات البناء التلقائي) ممكن يكون **مخفي** لو انت على Mac. لو ما ظهر معك بالسحب، تجاهله الآن وكمّل الخطوة الجاية لإنشائه يدوياً.
- اضغط **Commit changes** بالأسفل لرفع الملفات.

### 3) أضف ملف البناء التلقائي (لو ما انرفع تلقائياً)
- من صفحة المستودع اضغط **Add file → Create new file**.
- بخانة اسم الملف اكتب بالضبط: `.github/workflows/build-apk.yml`
  (GitHub بينشئ المجلدات تلقائياً لما تكتب مسار فيه `/`)
- افتح ملف `.github/workflows/build-apk.yml` الموجود جوه المشروع اللي عندك، وانسخ محتواه بالكامل، والصقه بمحرر GitHub.
- اضغط **Commit changes**.

### 4) شاهد البناء التلقائي
- روح لتبويب **Actions** أعلى صفحة المستودع.
- بيظهرلك عملية بناء (Workflow run) شغّالة تلقائياً — انتظر لحد ما تخلص (رمز ✅ أخضر)، بياخد عادة 2-5 دقايق.

### 5) حمّل ملف APK
- اضغط على عملية البناء المكتملة، ونزّل لتحت لحد قسم **Artifacts**.
- حمّل الملف `app-debug-apk` (بيجي بصيغة zip فيه ملف app-debug.apk جواه).
- انقل الملف لموبايلك (إيميل، جوجل درايف، USB...) وثبّته مباشرة (لازم تفعّل "تثبيت من مصادر غير معروفة" من إعدادات الموبايل أول مرة).

## ملاحظات مهمة

- **السيرفر يجب أن يكون شغّالاً** على Render حتى يعمل التطبيق (Render المجاني ينام بعد فترة من عدم الاستخدام، أول طلب ممكن ياخد 30-60 ثانية لإيقاظه).
- الموقع مضبوط بحيث أي رابط خارج نطاق موقعك (زي روابط خرائط جوجل) بيفتح تلقائياً بتطبيق/متصفح خارجي، وباقي الروابط تبقى جوه التطبيق.
- تم تفعيل صلاحية الموقع الجغرافي (GPS) لأن الكود يستخدمها في صفحة العميل.
- إشعارات المتصفح (`Notification` API) ما بتشتغل جوه WebView بشكل كامل — هاي محدودية طبيعية بهاي الطريقة. إذا احتجت إشعارات حقيقية توصل للكابتن/المطعم حتى لو التطبيق مقفول، هاد بده حل منفصل (Firebase Cloud Messaging) وممكن نضيفه لاحقاً.
- إذا غيّرت رابط الموقع مستقبلاً، عدّل قيمة `APP_URL` و `APP_HOST` في ملف:
  `app/src/main/java/com/adhna/fooddelivery/MainActivity.java`

## لإصدار ملف APK للتثبيت المباشر (بدون متجر)

في Android Studio: **Build → Build Bundle(s) / APK(s) → Build APK(s)**
بيطلع الملف من: `app/build/outputs/apk/debug/app-debug.apk`
