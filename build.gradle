plugins {
    id 'com.android.application' version '8.2.0' apply false
    // ⬇️ أضف هذا السطر الجديد ⬇️
    id 'com.google.gms.google-services' version '4.4.2' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
