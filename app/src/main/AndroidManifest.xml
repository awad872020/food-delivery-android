<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ [إضافة جديدة] إذن الوصول إلى الإنترنت (ضروري لتحميل المواقع) -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- ✅ [إضافة جديدة] إذن الإشعارات (ضروري لأجهزة Android 13 (API 33) فأحدث) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- نشاطك الرئيسي (الـ WebView) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ================================================== -->
        <!-- 🔥 [الإضافة الجديدة] خدمة استقبال إشعارات Firebase  -->
        <!-- ================================================== -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <!-- هذا الإجراء (ACTION) هو ما يخبر النظام بأن هذه الخدمة مسؤولة عن إشعارات FCM -->
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
